import 'package:flutter/material.dart';

import '../../model/usermodel.dart';

class Classp extends StatelessWidget {

  Classp({required this.user});
  UserModel user;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
