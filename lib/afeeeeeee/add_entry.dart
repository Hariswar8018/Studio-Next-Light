import 'package:flutter/material.dart';

class Add_Entry extends StatelessWidget {
  const Add_Entry({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body:
        Column(
            children: [

              ]
        )
    );
  }
}

